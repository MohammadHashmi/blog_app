server:
  port: 8080

spring:
    datasource:
        url: jdbc:postgresql://localhost:5332/blogs
        username: root
        password: root
    jpa:
        hibernate:
          ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true
        show_sql: true